<%= form_for(user_mail) do |f| %>
  <% if user_mail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_mail.errors.count, "error") %> prohibited this user_mail from being saved:</h2>

      <ul>
      <% user_mail.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :email %>
    <%= f.text_field :email %>
  </div>

  <div class="field">
    <%= f.label :login %>
    <%= f.text_field :login %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
